import React from "react";

export interface {{Domain}}Interface {
    // TODO
}

export default function {{Domain}}Component({ children }: React.PropsWithChildren<{{Domain}}Interface>) {
    return (
        <Fragment>
            { children }
        </Fragment>
    )
}