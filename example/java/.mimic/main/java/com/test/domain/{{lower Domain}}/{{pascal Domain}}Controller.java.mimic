package com.test.domain.{{lower Domain}};

import org.springframework.web.bind.annotation.*;

@RestController
@Path("/api/v1/{{kebab Domain}}")
public interface {{pascal Domain}}Controller {

    @PostMapping()
    public String create(@RequestBody {{pascal Domain}}Request {{camel Domain}}Request) {
        return "{{pascal Domain}} created"
    }

    @GetMapping("/{id}")
    public String read(@PathVariable String {{camel Domain}}Id) {
        return "{{pascal Domain}} found"
    }

}