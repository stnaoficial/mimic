package com.test.domain.{{ flat(domain) }};

import com.test.domain.{{ pascal(domain) }}Builder;

public class {{ pascal(domain) }}Entity {

    private final String id;

    public {{ pascal(domain) }}Entity({{ pascal(domain) }}Builder {{ camel(domain) }}Builder) {
        this.id = builder.id;
    }

    public String getId() {
        return this.id;
    }

}